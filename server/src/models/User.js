/**
 * models/User.js
 * Responsibility:
 *   - Schema for registered users (customer + admin).
 *   - Holds auth fields, profile, role, addresses,
 *     wishlist, view/search history, favorite genres.
 *   - Password hashing hooks; toJSON strips sensitive fields.
 */
// TODO: implement User schema, pre-save bcrypt, instance methods, indexes
module.exports = {};
