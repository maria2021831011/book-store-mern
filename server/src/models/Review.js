/**
 * models/Review.js
 * Responsibility: per-user-per-book review (rating + comment).
 * Used to compute Book.averageRating and reviewCount.
 */
// TODO: implement Review schema, unique index on (user,book)
module.exports = {};
