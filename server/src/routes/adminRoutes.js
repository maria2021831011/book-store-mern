/**
 * routes/adminRoutes.js — /api/admin/*
 *   /dashboard, /users, /books, /categories, /authors, /publishers,
 *   /orders, /inventory, /reviews, /coupons, /analytics,
 *   /ai/chat (Admin AI Assistant), /ai/analytics
 */
// TODO
const router = require("express").Router();
module.exports = router;