/**
 * routes/searchRoutes.js — /api/search/*
 *   GET /                keyword + filters + sort + pagination
 *   GET /semantic        semantic search (NLP)
 *   GET /autocomplete
 */
// TODO
const router = require("express").Router();
module.exports = router;