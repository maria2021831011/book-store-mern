/**
 * routes/userRoutes.js — /api/users/* (current user)
 *   GET /me, PUT /me, PUT /me/password
 *   GET /me/wishlist, POST /me/wishlist, DELETE /me/wishlist/:bookId
 *   GET /me/addresses, POST /me/addresses, PUT /me/addresses/:id, DELETE
 *   GET /me/history
 */
// TODO
const router = require("express").Router();
module.exports = router;