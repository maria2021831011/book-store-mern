/**
 * repositories/bookRepository.js
 * Responsibility: thin DB access layer for Book (find, paginate, byId,
 * searchWithFilters, updateStats). Keeps controllers lean.
 */
// TODO: implement query helpers used by book + recommendation + chatbot tools
module.exports = {};
