/**
 * ai/llm/toolSchema.js
 * Responsibility: tool/function definitions (JSON Schema) sent to the LLM.
 * Two sets: read tools and write tools (kept separate for guardrails).
 */
// TODO
module.exports = {};