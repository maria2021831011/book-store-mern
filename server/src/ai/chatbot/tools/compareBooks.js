/**
 * ai/chatbot/tools/compareBooks.js (READ tool)
 * Inputs: { bookIds: [string, string] }
 * Compares: topic, author, price, rating, pages, level, availability, description.
 */
// TODO
module.exports = {};