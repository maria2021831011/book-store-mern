/**
 * ai/chatbot/tools/searchBooks.js (READ tool)
 * Responsibility: keyword search via bookService.
 * Inputs: { q, category, author, minPrice, maxPrice, language, sort, page, limit }
 */
// TODO
module.exports = {};
