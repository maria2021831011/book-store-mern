/**
 * ai/rag/ragService.js
 * Responsibility:
 *   - retrieve relevant FAQ chunks for a user query (vector search)
 *   - pass them to LLM as grounded context
 *   - return cited answer
 *
 * Used for shipping, payment, returns, refunds, ordering, account help,
 * and store policies.
 */
// TODO
module.exports = {};