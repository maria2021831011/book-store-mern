/**
 * context/AuthContext.jsx
 * Responsibility:
 *   Holds current user + tokens; exposes login/register/logout helpers.
 *   Source of truth for role-based UI gating.
 */
// TODO: implement context with useReducer + localStorage persistence
export default function AuthProviderStub() {
  return null;
}